<% collection(:sketches).pages.each do |sketch| %>
  <a href="<%= sketch.url %>">
    <div class="sketch-link">
      <img src="<%= "/sketches/" + sketch.data[:cover] %>" class="thumbnail" />
      <span><%= sketch.data[:title] %></span>
    </div>
  </a>
<% end %>
